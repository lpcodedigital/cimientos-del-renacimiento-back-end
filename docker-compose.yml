services:
  cr-backend:
    build:
      context: .
      dockerfile: Dockerfile
    environment: 
      SPRING_PROFILES_ACTIVE: default
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs # <-- Sincroniza la carpeta local con el contenedor